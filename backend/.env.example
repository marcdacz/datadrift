# Encryption key for sensitive data source config (passwords, API tokens).
# Must be Base64-encoded, 32 bytes after decoding (AES-256).
# Generate with: openssl rand -base64 32
# Do not commit .env; use this file as a template only.
DATADRIFT_ENCRYPTION_KEY=REPLACE_ME
